---
/*
 * /privacy — Chromia-Cream legal page (kit Legal.jsx, kind="privacy").
 *
 * FLAG (content pass): this replaces the prior page's REAL legal content
 * (real sub-processors Plausible/Resend/Supabase, recruiting@iastaffing.com,
 * actual retention periods + US-state/GDPR notices) with the kit's generic
 * placeholder (imsmed.example). Replicated per the exact-replication directive
 * and gated from indexed production — restore/merge the real legal facts into
 * this design before launch.
 */
import MarketingLayout from '../layouts/MarketingLayout.astro';
import SiteNav from '../components/nav/SiteNav.astro';
import SiteFooter from '../components/footer/SiteFooter.astro';
import LegalPage from '../components/sections/LegalPage.astro';
---
<MarketingLayout
  title="Privacy — Innovative Medical Staffing"
  description="What we collect, how we use it, who can see it, and how to ask us to remove it."
  inCard={false}
>
  <SiteNav slot="nav" />
  <LegalPage kind="privacy" />
  <SiteFooter slot="footer" />
</MarketingLayout>
