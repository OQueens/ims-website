# Research Prompt — Net-New Moat (refreshes brief 21)

> Follow the shared guardrails in `README.md`. Refreshes `../briefs/21-innovation.md`.

## Scope
Differentiators nobody in locum staffing offers, ranked by moat strength × feasibility. Grounded in
the observation that **most of the wishlist is already 50-80% built and dormant** in this codebase.
The durable moat = (1) proprietary placement outcomes, (2) provenance-backed anti-gaming rails,
(3) cite-or-suppress honesty — none scrapable.

## Code anchors to re-verify (the dormant-moat inventory)
- `sim-adapter.ts:256-278` (confidence blend, LIVE); `marketRates.ts:448-456` (0 static 'high')
- Provenance: `rate_intelligence` `cited_text`/char-ranges (migration `20260602000000`), `aggregateBridge.ts:57-66` (Phase-8 deferral)
- Outcome loop: `quote_events`/`quote_outcomes` (`20260604000000`, unwired); `marketRates.ts:784-995` (calibration, 0 hub callers); `database.rules.json:26-47` (feedback world-writable — lock first)
- `sim-adapter.ts:372` (synthetic percentiles — negotiation readout blocked on Move #2)
- `types.ts:229-233` (call comp-model typing — unique but starved: 19/88, DAY drop)

## External questions to refresh
1. Competitive whitespace check (Marit, PSG, Vivian, Levels.fyi API-as-product precedent "$800/mo").
2. Antitrust posture for selling rate data to competitors (DOJ/FTC 2023 safe-harbor withdrawal — UNVERIFIED, counsel).
3. Verbatim-span republication legality (authed internal vs public).

## Deliverable
Rewrite brief 21 (ranked differentiator table moat×feas) + update BACKLOG rows for outcome loop,
"Explain this rate," negotiation readout, manual-controls fix, Locum Rate Index, headless core→API,
DAY/call path, anti-manip page, geo heatmap, Marit watch/partner.
