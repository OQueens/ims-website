# Sol adversarial-audit INPUT — LIVE rate simulator (2026-07-10)

North star: provable, un-gameable ACCURACY for real locum physician quotes at imstaffing.ai/hub. A wrong $/hr = a misquoted real placement. Live engine = `src/lib/rate-engine/*.ts` + `src/lib/hub/*.ts` (the ias-dashboard copy is a DEAD twin).

A prior 8-lens Claude review (adversarially self-refuted) produced the findings below. YOUR job as auditor: (1) for each CONFIRMED finding, read the cited file:line and independently confirm/refute + correct severity; (2) HUNT what was missed.


## CONFIRMED findings (17)


### C1. [high] src/lib/rate-engine/rateCalculator.ts:82  (lens=taxonomy)
- Defect: The alias-then-key substring scan (all ~140 aliases scanned before any of the 88 keys, both longest-first) still misroutes out-of-taxonomy specialties to a wrong cell and, unlike the IM default, tags them source:'inferred' so scoreConfidence (line 637) can read Medium/High on the wrong specialty.
- Failure: 'radiation oncology' -> alias 'oncology' (specialties.ts:288) -> medical oncology 350-500; 'cardiothoracic surgery' -> key substring 'thoracic surgery' -> 280-400; 'child neurology' -> alias 'neuro' (specialties.ts:268) -> adult neurology 180-275; 'ob hospitalist' -> key 'hospitalist' -> 145-240. Each returns source:'inferred' (lines 85/87/92); with a parsed state, scoreConfidence returns 'High' (line 634) — a confident wrong-specialty quote.
- Proposed fix: When a substring/alias hit does not consume specialty-bearing tokens in the input (e.g. 'radiation','child','cardiothoracic','laborist' left over), cap identification confidence at Low and flag for manual review rather than presenting Medium/High.
- Claude verify note: Verified in src/lib/rate-engine/rateCalculator.ts. mapSpecialty (lines 82-93) returns source:'inferred' for ANY alias/key substring hit; only the final fallthrough (line 94) returns 'default'. Confirmed against specialties.ts: 'radiation oncology' (no key, no 'radiation' alias) hits alias 'oncology' (line 288) -> medical oncology 350-500; 'ob hospitalist' hits key substring 'hospitalist' (line 74)

### C2. [medium] src/lib/rate-engine/rateCalculator.ts:94  (lens=taxonomy)
- Defect: mapSpecialty() still hard-defaults any unrecognized specialty to `internal medicine` (source:'default') with no manual-escalation surface — deep-dive defect #1 / brief-22 R2 is UNFIXED.
- Failure: A LocumSmart PDF (or manual text) with Specialty 'Certified Nurse Midwife', 'Spine Surgery', 'Transplant Surgery', or 'Addiction Medicine' matches no key/alias/substring and falls to line 94 -> quoted off the internal-medicine band p70 (spec.min 130 / max 200, specialties.ts:76). A spine/transplant surgeon or CNM is priced as a $130-200/hr internist — a large, plausible-looking underquote on a real placement.
- Proposed fix: Return a distinct source:'unmatched' (not 'default'->IM) and route it to an explicit 'unknown specialty - manual escalation' quote surface instead of silently pricing the IM band; log every fallback to quote_events so gap demand is measurable.
- Claude verify note: Verified rateCalculator.ts:94 — mapSpecialty() hard-returns { key: 'internal medicine', source: 'default' } after failing exact-key (80), alias (81), short-alias regex (82-89), and substring (90-92) matching. Confirmed the four claimed inputs genuinely fall through: grep of specialties.ts + all rate-engine files shows no key/alias for 'spine surgery', 'transplant surgery', 'certified nurse midwife

### C3. [medium] src/lib/hub/sim-adapter.ts:139  (lens=comp-factors)
- Defect: factorsFromControls hardcodes facility='community', call.hasCall=false, holiday.hasHoliday=false, so the hub's manual-controls flow (the most common recruiter path) cannot express three major price movers and silently prices every manual quote as a community, no-call, no-holiday assignment.
- Failure: Recruiter manually quotes anesthesiology for a Critical Access Hospital via the controls → engine uses FACILITY_MULT.community=1.00 instead of cah=1.22 → quote ~18% LOW with no warning. Conversely a telehealth assignment (FACILITY_MULT.telehealth=0.75) or academic (0.85) is quoted ~18-33% HIGH. A call-heavy manual quote loses the +10% call premium entirely. Only the PDF/freetext paths (simParseAssignment/simParseFreetext) exercise facility/call/holiday; the manual controls expose none.
- Proposed fix: Add facility-type and call/holiday controls to SimControls + factorsFromControls, or at minimum surface a visible 'assumes community facility, no call/holiday' disclaimer on manual quotes so the pinned defaults aren't invisible.
- Claude verify note: Confirmed in code: factorsFromControls (sim-adapter.ts:139/141/142) hardcodes facility='community', call.hasCall=false, holiday.hasHoliday=false; SimControls (lines 38-46) exposes no facility/call/holiday fields, so the manual UI cannot set them. FACILITY_MULT (multipliers.ts:23-35) confirms community=1.00, cah=1.22, telehealth=0.75, academic=0.85, matching the claimed 18-33% error magnitudes. The

### C4. [medium] src/lib/rate-engine/specialties.ts:288  (lens=taxonomy)
- Defect: The generic alias 'oncology'->'medical oncology' is longer than the specific 'gyn onc' alias (line 294) and is scanned before all keys, so it preempts more-specific oncology cells and systematically OVERquotes onc subspecialties to the top band ($350-500).
- Failure: 'gyn oncology' contains 'oncology' -> returns medical oncology 350-500 before the specific 'gyn onc' alias can fire (which only works for the exact string 'gyn onc'), instead of gynecologic oncology 260-370. Likewise 'pediatric oncology' -> medical oncology 350-500 (peds-onc is far lower), and 'radiation oncology' -> 350-500. A gyn/peds-onc locum is over-quoted by ~$90-150/hr.
- Proposed fix: Remove/narrow the bare 'oncology' alias or make it lowest-priority; add explicit aliases 'gyn oncology'/'gynecological oncology'->gynecologic oncology and route 'radiation oncology'/'pediatric oncology' to null->manual escalation until a cited band exists.
- Claude verify note: Traced the code end to end. Real triggering inputs confirmed: 'gyn oncology' and 'radiation oncology' both resolve to the 'oncology'->'medical oncology' alias, yielding a medium-confidence $350-500 quote. For 'gyn oncology' the correct cell is gynecologic oncology $260-370 (over-quote ~$130 on max, ~$100 on min); 'radiation oncology' has no curated band and is mislabeled as a premium medical-oncol

### C5. [medium] src/lib/rate-engine/specialties.ts:283  (lens=taxonomy)
- Defect: Alias 'endo'->'endocrinology' is not in the _shortAliases word-boundary set (rateCalculator.ts:75), so it matches via bare `.includes` inside 'endovascular', capturing surgical/interventional specialties into the endocrinology band.
- Failure: Specialty 'Endovascular Surgery' or 'Endovascular Neurosurgery' -> alias 'endo' fires in the longest-first scan before any key -> endocrinology 200-275, instead of vascular surgery 250-360 / neurointerventional 320-470. A high-value endovascular procedure specialty is mislabeled and underquoted by $50-250/hr, at source:'inferred' (Medium/High confidence).
- Proposed fix: Add 'endo' to _shortAliases so it requires a \bendo\b word boundary; add explicit 'endovascular ...' aliases to the correct surgical cells.
- Claude verify note: Verified in rateCalculator.ts:75-95. _shortAliases does not contain 'endo', so 'endo'->'endocrinology' matches via bare `lower.includes('endo')` in the step-3 alias scan (lines 82-88). For input "endovascular surgery": not an exact specialty/alias key; no alias longer than 'endo' is a substring; so the scan reaches 'endo' and returns endocrinology (200-275) with source:'inferred'. This pre-empts s

### C6. [medium] src/lib/rate-engine/rateCalculator.ts:658  (lens=calc-core)
- Defect: On the parsed (PDF/freetext) path the quote base is frozen at the pre-overlay p70, so the live market overlay's promoted anchor never becomes the quote base — only the ceiling moves.
- Failure: Recruiter parses a CRNA LocumSmart PDF: initFactors (rateCalculator.ts:901) captures baseRate = spec.p70 = 232 (curated). The Phase-2 overlay then promotes CRNA from fresh RTDB data with anchor = 280 (market runs hot): applyMarketBucketsOverlay sets spec.p70=280, spec.max=round(280×250/232)=302. hub-client.ts:252 re-quotes using the FROZEN pendingFactors, so calculateRate:658 computes base = f.baseRate(232) || spec.p70 — i.e. still 232 — while clamping to the new spec.max=302. Hero is anchored to the stale 232, ignoring the observed 280 central estimate → the placement is underquoted by up to ~$48/hr even though the whole point of the overlay was to anchor on that observed number. The cooler-market mirror (anchor 210) overquotes. The manual controls path is unaffected (factorsFromControls rebuilds baseRate=spec.p70 every update).
- Proposed fix: Either re-derive factors.baseRate from the current SPECIALTIES[key].p70 before each re-quote (hub-client.ts:252), or drop the frozen baseRate entirely and have calculateRate read base = spec.p70 fresh (no path currently passes a genuine custom base — both assignment sites set it to spec.p70), so the `f.baseRate ||` fallback only serves to freeze staleness.
- Claude verify note: Confirmed in code. initFactors (rateCalculator.ts:901) freezes baseRate=spec.p70 into the parsed factors; applyParse (hub-client.ts:377) stores them in pendingFactors, a frozen object cleared ONLY on a control edit. update() (hub-client.ts:252) re-quotes as `pendingFactors ?? factorsFromControls(controls)`, so a parsed-then-unedited assignment always reuses the frozen factors. calculateRate:658 `b

### C7. [medium] src/lib/rate-engine/rateCalculator.ts:675  (lens=comp-factors)
- Defect: A holiday-worked assignment is priced at a different premium (1.10x vs 1.35x) depending purely on which detector fires, because inferShiftEnhanced reads section VALUES only while hasHoliday reads the structured 'Holiday Coverage' field.
- Failure: PDF A has structured field 'Holiday Coverage: Yes' and 'Schedule: Day shift' → inferShiftEnhanced sees only value 'yes' (label stripped, line 214-216,231), shift stays 'day' (1.00x), hasHoliday=true → holidayMult=1.10x → total holiday premium 1.10x. PDF B has free-text notes 'holiday coverage required' → inferShiftEnhanced matches /\bholidays?\b/ (line 231) → shift='holiday' (SHIFT_MULT.holiday=1.35x, multipliers.ts:18), holidayMult neutralized → total 1.35x. Same real fact (physician works holidays), ~22% different premium on every hour. The authoritative structured field yields the SMALLER premium.
- Proposed fix: Reconcile the two holiday signals: either (a) drive both off a single resolved holiday factor so structured and free-text detection yield the same premium, or (b) if 1.10x ('some holidays in a multi-day assignment') vs 1.35x ('all-holiday shift') are genuinely distinct cases, gate them on span/duration evidence rather than parse channel. Cite whichever premium is chosen in RATE_SOURCES.
- Claude verify note: Confirmed in code. buildFactors (rateCalculator.ts:895,899) feeds the hourly calculateRate two independent holiday detectors: shift=inferShiftEnhanced and holiday=hasHoliday. inferShiftEnhanced (lines 214-216, 231) builds allText from section VALUES only (label stripped) + notes, and fires key='holiday' (SHIFT_MULT.holiday=1.35, multipliers.ts:18) on any bare /\bholidays?\b/. hasHoliday (lines 389

### C8. [medium] src/lib/rate-engine/rateCalculator.ts:227  (lens=comp-factors)
- Defect: inferShiftEnhanced classifies any 24-hour / 24hr / 24/7 hourly assignment as shift='night' (SHIFT_MULT.night=1.20x), applying a full 20% night premium to every hour of a shift that actually spans day and night.
- Failure: An hourly (not call-only-detected) assignment described as '24/7 ED coverage' or '24-hour shifts' hits line 227 first → shift='night' → 1.20x applied to all hours. A 24-hour shift averages day+night, so pricing all hours at the night premium over-quotes by roughly the day-fraction of the shift (~15-20%). It also short-circuits BEFORE the weekend_night check (line 228), so a '24-hour weekend' loses the weekend classification too.
- Proposed fix: Either introduce a dedicated 24hr/blended premium below the pure-night 1.20x, or reorder so weekend/day context is considered; cite whatever multiplier is used.
- Claude verify note: Confirmed at rateCalculator.ts:227. The regex /\b(24[- ]?hour|24[- ]?hr|24\/7)\b/ tests `combined` (Coverage Type + Schedule/Shift/Hours + all section values + notes) and returns {key:'night'}, which maps to SHIFT_MULT.night=1.20 (verified multipliers.ts:15) and is applied at line 670. It short-circuits BEFORE weekend_night (228), holiday (231), night (232), weekend_day (233), and day-shift (234).

### C9. [medium] src/lib/rate-engine/marketRates.ts:519  (lens=market-overlay)
- Defect: A promoted anchor moves the quote with NO plausibility ceiling relative to the researched curated prior — the researched band (the one sanity envelope) is deliberately widened/rescaled to fit the anchor (lines 528, 543-545) rather than bound it, so a corroborated-but-wrong posterior mean sets an arbitrary base and ceiling.
- Failure: A fresh v2 bucket for a $250-market specialty with rateType=actual_paid_locum, n_distinct=5, source_families=['a','b'], weighted_mean=900 (e.g. two families both scraping aspirational 'up to $X' ranges recovered to distinct families, or a systematic classification/data-quality skew) clears every gate. anchor=900 → spec.p70=900, spec.max=round(900*max/p70). calculateRate uses base=900, and the only ceiling (spec.max) was just derived FROM the anchor, so a default day-shift assignment quotes ~$900/hr for a $250 specialty, labeled 'high' confidence + 'live'.
- Proposed fix: Add a sanity guard before promotion: reject (or clamp + downgrade confidence) when anchor exceeds a plausibility multiple of range.max (e.g. anchor > 1.5*range.max), so the researched prior remains a magnitude backstop even for corroborated anchors. Surface such cases to manual review rather than auto-anchoring.
- Claude verify note: Confirmed in marketRates.ts applyMarketBucketsOverlay (519-551) and rateCalculator.ts calculateRate (680-707). After the corroboration gates (anchorable rateType, finite weighted_mean>0, n_distinct>=4 integer, >=2 distinct families), anchor=round(weighted_mean) is written to spec.p70 unconditionally — there is NO check of anchor against range.min/range.max. spec.min=Math.min(range.min,anchor); spe

### C10. [low] src/lib/hub/sim-render.ts:68  (lens=percentiles)
- Defect: The market-position legend still renders "Premium tier $X" where $X is the uniform-interpolated p90 of the curated band (min+0.9·(max−min)), dressing a pure geometry artifact of a mixture band as a distinct observed market segment — the last surviving display-layer residual of deep-dive defect #2, which the chips (relabeled "% of range") and marker (relabeled "Recommended") already fixed.
- Failure: Anesthesiology, National, Standard → curated band [300,400]. sim-adapter.ts:372 yields p90=390. marketHTML gates on p90>marketMarker(≈370) so the legend shows "Premium tier $390". A recruiter pricing a subspecialty/urgent cardiac-anesthesia premium reads "$390" as the observed premium market rate and quotes it, but $390 is just 90% up a curated, comp-factor-mixed band (brief F5); real premium cardiac anesthesia can exceed the curated $400 max entirely, so the shown tier both mislabels a geometry point as a market segment and can understate a genuine premium.
- Proposed fix: Relabel to a band-position phrase consistent with the chip patch, e.g. "Upper band (90% of range) $390", or drop the dollar value and show only the zone; update the sim-render.ts:44-49 comment that still calls it "where premium agencies and urgent/subspecialty assignments land". Zero math change.
- Claude verify note: Verified sim-adapter.ts:372 with WF_PERCENTILES=[25,50,70,75,90] (line 254): percentiles are pure uniform linear interpolation Math.round(adjustedMin + (adjustedMax-adjustedMin)*(p/100)), so p90 = min+0.9*(max-min) — a geometry point, exactly as claimed. Verified sim-render.ts:68 renders `Premium tier ${usd(p90)}` gated on p90 > marketMarker (line 62), while the chips (line 58) were relabeled to t

### C11. [low] src/lib/rate-engine/marketRates.ts:513  (lens=anti-manip)
- Defect: The family-count filter drops only empty/blank strings, not the non-independent sentinel families ('unknown' / 'unattributed') that denote 'we could not attribute this source'. Such sentinels are counted as a legitimate independent corroborating family.
- Failure: A single real agency ('amn') advertising inflated pay across 4 postings, plus a batch of aggregator rows the bridge could not attribute (collapsed to the 'unattributed' family, per dedup design), yields source_families:['amn','unattributed'], n_distinct:5. The engine counts 2 distinct non-empty families, clears minFamilies=2, and anchors 'high' — even though only ONE real independent source exists and the second 'vote' is literally 'unknown'. The deep-dive assumed unattributed-alone fails the gate (safe), but one real family + unattributed noise passes it.
- Proposed fix: Add a NON_INDEPENDENT_FAMILIES denylist ({'unknown','unattributed'}) and exclude those from the distinct-family count in the gate, mirroring the exclusion-by-absence idiom already used for never-anchor sources.
- Claude verify note: Verified marketRates.ts:510-517 (applyMarketBucketsOverlay, the SOLE live overlay). The family-count gate builds its Set from b.source_families with only `.filter(s => typeof s === 'string' && s.trim().length > 0)` — it drops blank/whitespace but NOT the semantic-null sentinels 'unknown'/'unattributed'. Crucially, unlike the RETIRED loadMarketRates path (lines 712-721) which derives families itsel

### C12. [low] src/lib/rate-engine/rateCalculator.ts:659  (lens=bls-geo)
- Defect: Live geography is one state-level scalar (STATE_MULT, 0.88–1.38); ruralMult is pinned 1.0 (rateCalculator.ts:669) and the genuine rural premium (FACILITY_MULT cah 1.22 / rural_trauma 1.30) is unreachable from the hub manual sim because facility is hardcoded 'community' (sim-adapter.ts:139). No sub-state metro/rural dimension exists on the live path; BLS OEWS (the one metro-granular source in-repo) feeds only the dead sanity layer.
- Failure: Recruiter prices a critical-access-hospital assignment in the hub: cannot express the CAH 1.22× premium (facility fixed to community), and a Manhattan-NY vs rural-upstate-NY job receive the identical STATE_MULT[NY] — the largest legitimate geographic premium the engine models is not expressible in the main flow.
- Proposed fix: Expose the existing CAH/rural_trauma facility control in the hub sim (cheapest fix); longer term add the geo_tier ladder from deep-dive brief 26. Known limitation — confirm, don't necessarily fix now.
- Claude verify note: Confirmed all cited facts by reading the code. rateCalculator.ts:669 pins ruralMult=1.0 (intentional, to avoid double-counting rural economics already inside STATE_MULT). FACILITY_MULT.cah=1.22 and rural_trauma=1.30 (multipliers.ts:28,33) are the largest geo-adjacent premiums. sim-adapter.ts:139 hardcodes facility:{key:'community'} for the manual-controls path, and the visible SimControls expose o

### C13. [low] src/lib/rate-engine/parser.ts:276  (lens=taxonomy)
- Defect: Freetext cleaning strips hyphens to spaces (`replace(/[^a-z0-9\s/]/g,' ')`) but keeps '/', so hyphen/space spellings of slash-form specialties never match and buildParsedFromFreetext returns null (line 359) -> no quote.
- Failure: 'ob-gyn nights in tx' -> tokens ['ob','gyn']; fuzzyMatchSpecialty rejects 'ob' and 'gyn' (both <4 chars, no exact key) and the phrase 'ob gyn' matches nothing (the key is 'ob/gyn' with a slash) -> specialty null -> buildParsedFromFreetext returns null -> the recruiter gets NO quote for IAS's #1-volume specialty. 'obgyn' and 'ob/gyn' both work; only the extremely common hyphen/space form fails.
- Proposed fix: Normalize separators (map both '-' and '/' to a canonical form, or add 'ob gyn'/'ob-gyn' aliases) so slash-form keys match their hyphen/space spellings.
- Claude verify note: Confirmed in code. parser.ts:276 `.replace(/[^a-z0-9\s/]/g,' ')` strips '-' to space but preserves '/'. For 'ob-gyn ...' the input becomes 'ob gyn ...'. The canonical key is 'ob/gyn' (specialties.ts:114) with a slash; grep confirms there is NO 'ob gyn' or 'ob-gyn' alias (only 'obgyn' with no separator, line 289). Multi-word match tries phrase 'ob gyn' but fuzzyMatchSpecialty finds no exact key/ali

### C14. [low] src/lib/rate-engine/rateCalculator.ts:673  (lens=comp-factors)
- Defect: hasCall on the hourly path is a flat, uncited 1.10x that prices all call burden identically and cannot be varied — a 'q2 in-house' and a '1:4 shared beeper' both add exactly 10%, and the manual controls can't set it at all.
- Failure: Two hourly assignments, one with brutal q2 in-house call and one with light 1:4 beeper call, both parsed from freetext → both get callMult=1.10x, identical quote. The 1.10 constant has no inline citation (nearest provenance RATE_SOURCES.shift). Materially under-prices heavy in-house call burden on the hourly path.
- Proposed fix: Tier the call multiplier by restriction/burden with cited differentials, or keep 1.10 but label it ESTIMATED in RATE_SOURCES and on the surface.
- Claude verify note: Verified rateCalculator.ts:673 — callMult is a flat binary `f.call.hasCall ? 1.10 : 1.0`. hasCall() (lines 323-337) returns a pure boolean; its regex recognizes call-ratio/beeper/shared-call phrasing but discards all magnitude, so q2 in-house and 1:4 beeper both yield hasCall:true → identical 1.10x. RATE_SOURCES (callRates.ts:408) has a `shift` entry but no `call` entry, so 1.10 is uncited (neares

### C15. [low] src/lib/rate-engine/marketRates.ts:295  (lens=bls-geo)
- Defect: readCellBuckets prefers the 'national' cell, else falls to the FIRST state cell in key order without reference to the quote's selected state (deep-dive A3, rec 2). Latent because live v2 is 100% national, but activates as soon as real state cells arrive.
- Failure: Once the fleet writes any state-attributed cells, a user quoting FL could silently receive the first-key state cell (e.g. AK) as their market anchor, mispricing the quote — an arbitrary-cell overlay independent of the user's state.
- Proposed fix: Pass the quote's state into readCellBuckets and prefer (state cell)→national instead of national→first-key state cell.
- Claude verify note: Confirmed in marketRates.ts:284-314. readCellBuckets(specNode) takes no state argument; lines 293-303 prefer the 'national' cell, and if absent iterate Object.keys(node) skipping __meta__/national and take the FIRST state cell with a valid buckets node (break) — arbitrary key-order selection with zero reference to the quote's state, exactly as claimed. It is genuinely latent: (1) live v2 is 100% n

### C16. [low] src/lib/rate-engine/specialties.ts:263  (lens=taxonomy)
- Defect: Alias 'icu'->'critical care' lacks a word boundary (not in _shortAliases) and, because aliases outrank keys, a pediatric ICU PDF specialty resolves to adult critical care; refineSpecialtyFromContext only rescues inputs already mapped to pediatrics, not to critical care.
- Failure: PDF Specialty field 'Pediatric ICU' -> mapSpecialty alias 'icu' -> critical care 190-300 (adult). refineSpecialtyFromContext (rateCalculator.ts:105) only reroutes key=='pediatrics', so it never fires -> quoted as adult critical care instead of pediatric critical care 180-260. (Freetext path is safe: it maps 'pediatric'->pediatrics then refine catches the ICU context.)
- Proposed fix: Add 'icu' to _shortAliases (\bicu\b) and add a refine rule mapping critical care -> pediatric critical care when pediatric context tokens are present.
- Claude verify note: Verified in rateCalculator.ts (mapSpecialty :77-95, refineSpecialtyFromContext :105-134, invoked :879-880) and specialties.ts alias/table. Input specialty field "Pediatric ICU" -> lower "pediatric icu": no exact key/alias; in the _sortedAliases scan 'icu' is not in _shortAliases (line 75), so plain includes('icu') matches -> returns 'critical care' (adult 190-300, medium). No longer alias matches 

### C17. [low] src/lib/rate-engine/rateCalculator.ts:704  (lens=calc-core)
- Defect: The spec.max researched-ceiling clamp (CLAMP 3) erases all shift/geo/facility/duration premiums for narrow curated bands whenever base×geo alone already reaches spec.max — intended, but it silently makes distinct assignments quote identically.
- Failure: CRNA (p70=232, max=250 — only 7.7% headroom). Any state with geoMult>250/232≈1.078 pushes base×geo above the ceiling, so a plain CRNA DAY shift and a CRNA WEEKEND-NIGHT shift (1.30×) in the same high-cost state BOTH clamp to $250 — the legitimate ~30% night/weekend premium is invisible in the hero. Only the marketMaxApplied flag hints at it. Because the curated max is itself an ESTIMATE, hard-capping premiums beneath it is a defensible design choice but produces a real accuracy artifact for every narrow-band specialty.
- Proposed fix: No code change if this is the accepted design; if premiums should price above the curated max, apply the same geometry-rescale used for hot overlay anchors (marketRates.ts:543) to widen the ceiling when a stacked-premium quote would exceed it, or widen narrow curated bands so p70→max headroom reflects the true premium range.
- Claude verify note: Confirmed by reading the code. CRNA band min:190/max:250 (specialties.ts:59) gives p70=Math.round(190+(250-190)*0.70)=232 (specialties.ts:179), matching the claim. base defaults to spec.p70 (rateCalculator.ts:658). Shift mults day:1.00 vs weekend_night:1.30 (multipliers.ts:14,17). State mults derive up to a 1.38 clamp (stateData.ts:170); high/critical low-cost states readily exceed the 1.078 thres

## REFUTED / downgraded by the Claude pass (14) — do NOT re-raise unless you disagree with the refutation

- src/lib/rate-engine/rateCalculator.ts: Displayed percentiles are linear interpolation across [min,max], a uniform-distribution fabrication, not an ob -- REFUTED: Read rateCalculator.ts:50-53 (getPercentileRate) and grepped all call sites: it is exported via index.ts:16 but has NO live consumer (sim-adapter.ts:372 inlines its own i
- src/lib/rate-engine/rateCalculator.ts: On the call-only path, dayPay clamps typical×cappedMult to the researched band max, so for narrow/single-point -- REFUTED: Read rateCalculator.ts:790-823 (dayPay clamp + flag exposure), callRates.ts (band data — single-point/narrow bands confirmed), multipliers.ts:23-44 (facility/duration mul
- src/lib/rate-engine/blsSanityCheck.ts: The BLS sanity-check layer and its per-specialty over-quote ceiling (MAX_HOURLY_CEILING, incl. crna=250) have  -- REFUTED: Whole-repo grep: evaluateBlsSanityCheck appears only at its definition (blsSanityCheck.ts:373), the barrel export (rate-engine/index.ts:27), and test files; MAX_HOURLY_CE
- src/lib/rate-engine/marketRates.ts: The live anchor gate counts distinct trim+lowercase source_families[] strings verbatim and never re-applies so -- REFUTED: Confirmed at marketRates.ts:510-516 that the live anchor gate (applyMarketBucketsOverlay) counts families via new Set(source_families.map(s=>s.trim().toLowerCase())).size
- src/lib/rate-engine/rateCalculator.ts: The ONLY live over-quote ceiling is `payRate > spec.max` clamped to the single national curated max (CRNA spec -- REFUTED: Verified rateCalculator.ts:704 — the `payRate > spec.max` clamp is real and does bind CRNA (spec.max=250, specialties.ts:59) once base(p70)*geoMult*premiums exceed 250. B
- src/lib/rate-engine/marketRates.ts: The >=2-family corroboration gate counts distinct normalized source_families strings but does NOT apply the so -- REFUTED: Verified marketRates.ts:510 (v2 overlay) vs :761 (legacy). The asymmetry is intentional and documented at lines 106-110: the v2 reader is a PURE consumer of server-comput
- src/lib/hub/sim-render.ts: The marketHTML doc comment still describes the shipped honesty-patched UI in the OLD dishonest terms — "percen -- REFUTED: Confirmed at src/lib/hub/sim-render.ts:43-49 the doc comment does use older terms ("percentile chips (p70 highlighted, as in the dashboard)" and "recommended (≈market-med
- src/lib/rate-engine/rateCalculator.ts: callbackRate and includedHrs (gratis hours) are returned on CalculatedCallRate as display-only context and mov -- REFUTED: Verified in rateCalculator.ts: dailyPay (lines 790-806) is band.typical × cappedMult clamped to researched-max/rate-cap; callbackRate and includedHrs (833-834) are comput
- src/lib/rate-engine/specialties.ts: The load-bearing 'no static high' invariant that the entire confidence-honesty design depends on is unenforced -- REFUTED: Verified the full chain. The mechanism is real: SpecialtyRateInput.confidence (specialties.ts:52) permits 'high', the header (lines 6-15) documents static high/medium/low
- src/lib/hub/sim-render.ts: Among the honestly-relabeled "% of range" chips, the 70% chip alone gets the `is-p70` cyan highlight (styled a -- REFUTED: Verified in src/lib/hub/sim-render.ts:57-58, sim-adapter.ts:254/371/372, and hub.css:251. The is-p70 highlight is CSS-only (a cyan tint) and affects no quote number. The 
- src/lib/rate-engine/sourceFamily.ts: sourceFamily() splits only on underscore, so hyphen/space/dot/concatenation variants of a KNOWN_FAMILY_OVERRID -- REFUTED: Verified in src/lib/rate-engine/sourceFamily.ts:216-234: sourceFamily() splits only on '_', so sourceFamily('cross-country') returns 'cross-country' (fallback) while sour
- src/lib/rate-engine/marketRates.ts: The anchor is the posterior weighted MEAN (a central tendency ~50th pct) but is written into spec.p70, which t -- REFUTED: Verified specialties.ts:179 (curated p70 = round(min+(max-min)*0.70), a true 70th-pct-of-band) and marketRates.ts:519,527 (anchor = round(weighted_mean), written to spec.
- src/lib/rate-engine/marketRates.ts: advertised_clinician_pay is treated identically to actual_paid_locum — both anchorable and both labeled 'high' -- REFUTED: Read marketRates.ts:113-129, 419-461, 474-517. The code does classify advertised_clinician_pay as anchorable and 'high'-capped, but this is an explicit, documented design
- src/lib/rate-engine/marketRates.ts: The deep-dive premium-erasure defect (a hot anchor collapsing spec.max onto p70 and flattening every night/wee -- REFUTED: Confirmed the premium-erasure defect is fixed and sound in current code — no current input produces a flattened/wrong quote. Verified: (1) rateCalculator.ts:704 clamps ev

## Lens health summaries

- **taxonomy**: The specialty taxonomy/mapping lens is UNHEALTHY and deep-dive defect #1 is fully LIVE, not fixed. mapSpecialty (rateCalculator.ts:77-95) still hard-defaults unrecognized specialties to `internal medicine` and still resolves via a broad alias-then-key substring scan, so every F5 trace (radiation oncology, cardiothoracic surgery, child neurology, ob hospitalist, CNM, spine surgery, addiction medicine) still misprices exactly as documented; the recommended R2 manual-escalation surface was never built (no `unknown specialty` handling exists in src/lib). Worse, because misfolds carry source:'infer
- **calc-core**: The input→$ pipeline is largely sound and the premium-erasure bug it was famous for is genuinely FIXED: marketRates.ts:543-545 rescales spec.max geometrically (round(anchor × range.max/range.p70)) whenever a promoted anchor meets/exceeds the curated max, so p70===max collapse can no longer flatten shift/geo/facility premiums back onto the base. Multiplier order, the 1.75 combined-mult cap, the rate-cap×0.80 clamp, the spec.max ceiling, and rounding are all internally consistent and honestly flagged (capped / marketMaxApplied). The one real NEW defect is a data-flow bug: on the PARSED (PDF/free
- **market-overlay**: The trust-ladder overlay is one of the most carefully-built and best-tested surfaces in the engine (marketBucketsOverlay.test.ts pins ~20 gate/band branches), and the deep-dive's headline premium-erasure defect is genuinely FIXED and sound: the rescale ternary at marketRates.ts:543-545 preserves premium headroom, and the invariant it depends on (range.p70 < range.max) provably holds for all 88 curated specialties (p70 = round(min+0.70*(max-min)), every band has max-min >= 25, so p70 <= max-7 always). The remaining findings are not miscomputations but design-level accuracy gaps: the corroborati
- **percentiles**: Deep-dive defect #2 (displayed percentiles are linear interpolation of a curated [min,max] band, presented as observed quantiles) is CONFIRMED still present in the MATH — sim-adapter.ts:372 computes `value = min + (max-min)*(p/100)` over the static curated band (computeAdjustedSpecRange, rateCalculator.ts:752, returns the unscaled spec.min/spec.max), and specialties.ts:179 builds p70 the same way. HOWEVER, the DISPLAY-layer dishonesty the brief flagged has been largely FIXED by a shipped Phase-0 honesty patch: the chips now read "X% of range: $Y" (sim-render.ts:58, honest fraction-of-range fra
- **comp-factors**: The comp-factor/premium math is mostly coherent and the two headline deep-dive concerns in this lens are handled honestly: the premium-erasure bug IS fixed (marketRates.ts:543-545 re-scales spec.max geometry when a live anchor >= curated max, so shift/geo/facility/duration premiums still price above the base), and the daily/RVU "mishandling" (deep-dive defect #4) does NOT manifest as a fabricated hourly number inside this engine — baseRate is always spec.p70, never a parsed posted dollar, so a $2,000/day posting is never misread as $2,000/hr; the drop happens in the out-of-repo scraper. The re
- **confidence**: The confidence-honesty lens is genuinely sound in current code: no curated/uncorroborated band can display 'high'. All static bands are medium/low/modeled (no static 'high' remains), the retired loadMarketRates high-path is unwired in prod (only loadMarketBucketRates runs), and the live overlay only grants 'high' to fresh, market-typed posteriors with n_distinct>=4 and >=2 normalized-deduped families. The display blend (weaker of identification vs data tier) correctly caps a fully-identified quote at Medium for every un-promoted specialty. The single defect is latent, not live: the whole guara
- **anti-manip**: The headline anti-manipulation defect for this lens — whitespace/case variants of ONE family faking distinct independent votes (deep-dive T6) — is FIXED and live: applyMarketBucketsOverlay normalizes each source_families[] entry with .trim().toLowerCase() and empty-filters before Set-counting (marketRates.ts:510-517), so ['exa',' exa ','EXA'] collapses to one vote. However, the live anchor gate is the ONLY source-independence check that actually runs on a quote, and it counts distinct raw family strings VERBATIM — it never re-applies sourceFamily()/KNOWN_FAMILY_OVERRIDES corporate-family colla
- **bls-geo**: The BLS sanity-ceiling layer this lens is scoped to (blsSanityCheck.ts, MAX_HOURLY_CEILING with crna=250, and the entire BLS OEWS baseline) is NOT wired into the live simulator: evaluateBlsSanityCheck / MAX_HOURLY_CEILING / BLS_OEWS_BASELINE have zero consumers outside the module's own barrel re-export and tests (the deep-dive's referenced src/features/rate-simulator/components/MarketContext.tsx does not exist in this repo). So the ceilings neither silently clamp legitimate rates nor let garbage through — they are inert; the only over-quote backstop that actually runs live is spec.max in rateC